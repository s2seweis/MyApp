
namespace MyApp
{
    internal class IfStatements
    {
        public static void CalculateIfStatements()
        {

            ////int number = int.Parse(Console.ReadLine());
            //int number = 10;

            ////values can be true or false, if true code will be executed
            //if (number > 0) 

            //{ 
            //    Console.WriteLine("Ist größer als 0");
            //}

            // #######################################################

            string name = "Sebastian";

            if (name == "Sebastian")
            {

                Console.WriteLine("Es ist Sebastian!");
            }

            // #######################################################

            // Operatoren:
            // > größer als
            // >= größer gleich
            // < kleiner als
            // <= kleiner gleich
            // == ist geich
            // != ungleich
            // || oder
            // && und

            // Hinweis, einfaches = setzt einen Wert

            // go to Operators.cs ==> for see more if statements

        }
    }
}
